info:
  name: Cria
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/corretora-cia
  body:
    type: json
    data: |-
      {
        "corretoraId": 3,
        "ciaId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
